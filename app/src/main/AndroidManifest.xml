<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.svoemestodev.fdmcostcalculator">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="q">

        <activity
            android:name=".MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:launchMode="singleInstance" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".ProductActivity"
            android:label="@string/product" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/setting" />
        <activity
            android:name=".FilamentActivity"
            android:label="@string/filament" />
        <activity
            android:name=".PlasticActivity"
            android:label="@string/plastic" />
        <activity
            android:name=".ListProductsActivity"
            android:label="@string/products" />
        <activity
            android:name=".ListSettingsActivity"
            android:label="@string/settings" />
        <activity
            android:name=".ListFilamentActivity"
            android:label="@string/filaments" />
        <activity
            android:name=".ListPlasticActivity"
            android:label="@string/plastics" />
        <activity
            android:name=".PartActivity"
            android:label="@string/part" />
        <activity
            android:name=".ListPartsActivity"
            android:label="@string/parts" />



        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2870168293382158~5551394444" />
    </application>

</manifest>